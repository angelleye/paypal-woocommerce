<?php
!defined('ABSPATH') && die('Direct access not allowed');

// This name shouldn't be changed as PayPal uses same param name to set the approval_token_id when that redirects from PayPal website
const APPROVAL_TOKEN_ID_PARAM_NAME = 'approval_token_id';
